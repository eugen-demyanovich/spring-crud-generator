package ${package}.repository;

import ${package}.model.${entityName};
import org.springframework.data.repository.CrudRepository;

public interface ${entityName}Repository extends CrudRepository<${entityName}, Long> {
}
